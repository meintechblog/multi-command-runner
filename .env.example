# Server bind settings
HOST=127.0.0.1
PORT=8080

# Optional: custom data directory for app.db/logs/runtime status
DATA_DIR=/opt/multi-command-runner/data

# Optional but recommended: explicit secret for credential encryption.
# If not set, the app uses/creates DATA_DIR/.credentials.key.
MULTI_COMMAND_RUNNER_SECRET_KEY=

# Optional but strongly recommended: enable HTTP Basic auth for UI/API.
# Set both values to enable protection. If only one is set, auth stays disabled.
MULTI_COMMAND_RUNNER_AUTH_USER=
MULTI_COMMAND_RUNNER_AUTH_PASSWORD=
